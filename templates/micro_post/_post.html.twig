<div class="bg-white p-4 shadow rounded-lg">
    <div class="mb-4">
         <div class="text-2xl">
            <a href="{{ path('app_micro_post_show', { post: post.id }) }}" class="text-blue-500 hover:underline">
                {{ post.title }}
            </a>
        </div>
        <div class="text-xl dark:text-gray-400">
            {{ post.text }}
        </div>
        <div class="text-gray-500 dark:text-gray-400">
            {{ post.created | date("M j, Y") }}
        </div>
        <div>
            <a href="#" class="text-gray-500 dark:text-gray-400">
                Like<span class="px-2 rounded-md bg-gray-200 dark:bg-gray-700">0</span>
            </a>
        </div>
        <div class="text-sm text-gray-500 dark:text-gray-400">
            <a href="#">user@email.com</a>
        </div>
        <div class="text-sm text-gray-500 dark:text-gray-400">
            <a href="{{ path('app_micro_post_edit',{post: post.id}) }}">Edit</a>
        </div>
    </div>
</div>